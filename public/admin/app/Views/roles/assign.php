<form method="POST" action="/roles.php?action=assign">
    <select name="user_id">
        <!-- Opciones de usuarios -->
    </select>
    <select name="role_id">
        <!-- Opciones de roles -->
    </select>
    <button type="submit">Asignar</button>
</form>